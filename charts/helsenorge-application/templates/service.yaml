apiVersion: v1
kind: Service
metadata:
  name: {{ include "helsenorge-application.fullname" . }}
  labels:
    {{- include "helsenorge-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helsenorge-application.selectorLabels" . | nindent 4 }}
