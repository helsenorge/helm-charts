apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "application.fullName" . }}-test-connection"
  labels:
    {{- include "helsenorge-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "application.fullName" . }}:{{ .Values.service.port }}/api/health']
  restartPolicy: Never
